// 아기사자 한명을 Lion 객체로 표현
// 이름, 전공, 기수

// 유효성 검증 로직을 main vs 객체의 도메인 클래스
// -> main 과 객체 중 누가 유효성 검증을 책임지는지에 따라 구조가 달라진다.

// 접근제어자로 객체 내부 데이터 보호르 컴파일 결과로 체감
// -> 접근하는 위치가 같은 패키지인지 다른 패키지인지에 따라 필드 접근 여부가 달라진다.

// 클래스의 책임이 main 이 아니라 클래스에 있어야하는지 설명.

// 이름, 전공, 기수 입력

// 유효한 경우, 생성된 아기사자의 이름, 전공, 기수 정보 콘솔에 출력
